<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="MyThemeLight" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MyThemeDark" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- activity_information.xml使用 -->
    <style name="CustomRadioButton">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/rb_nobtn_selector</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@drawable/rb_text_color_selector</item>
    </style>

    <!-- activity_shopping.xml使用 -->
    <style name="ShoppingRadioButton">
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@drawable/rb_text_color_selector</item>
        <item name="android:drawableRight">@drawable/rb_shopping_arrow_selector</item>
    </style>

    <!-- activity_annual_check_fill_info.xml使用 -->
    <style name="AnnualCheckRadioButton">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/btn_cycrect_blue_gray_selector</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@drawable/rb_text_white_gray_selector</item>
        <item name="android:padding">2dp</item>
    </style>

    <style name="CustomButton">
        <item name="android:background">@drawable/btn_selector</item>
        <item name="android:textColor">@drawable/btn_text_color_selector</item>
    </style>

    <style name="CustomButtonCycRect">
        <item name="android:background">@drawable/btn_cycrect_selector</item>
        <item name="android:textColor">@drawable/btn_text_color_selector</item>
    </style>

    <style name="CustomCheckBox">
        <item name="android:button">@null</item>
    </style>

    <style name="MainRatingStyle" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_main_selector</item>
        <item name="android:minHeight">12dp</item>
        <item name="android:maxHeight">12dp</item>
        <item name="android:paddingRight">4dp</item>
    </style>

</resources>
